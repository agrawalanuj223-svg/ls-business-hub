"use client";

import { ClientShell } from "@/components/portal-shells";
import { Badge, Card, CardContent, CardHeader, ErrorBlock, LoadingBlock, SectionTitle } from "@/components/ui";
import { useClientInvoiceSummary, useClientMemberships } from "@/hooks/client/useClientBusinessData";

const inr = new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 });
const demoInvoiceId = "50000000-0000-4000-8000-000000000001";

export default function ClientDashboardPage() {
  const memberships = useClientMemberships();
  const invoice = useClientInvoiceSummary(demoInvoiceId);
  const loading = memberships.isLoading || invoice.isLoading;
  const error = memberships.isError || invoice.isError;
  const row = invoice.data;
  return <ClientShell><div className="py-4 lg:py-8"><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><Badge tone="blue">PRIVATE CLIENT SPACE</Badge><h2 className="mt-4 text-3xl font-bold tracking-tight">Your business at a glance</h2><p className="mt-2 max-w-xl text-sm leading-6 text-slate-500">Welcome to your secure organization view. Information shown here is limited to your authorized client membership and shared records.</p></div><div className="rounded-2xl border border-blue-100 bg-blue-50 px-4 py-3 text-right"><p className="text-xs font-semibold uppercase tracking-wider text-blue-600">Organization</p><p className="mt-1 font-bold text-blue-950">DEMO CLIENT ALPHA</p></div></div>{loading ? <LoadingBlock label="Loading your private client workspace…" /> : error ? <ErrorBlock /> : <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><ClientMetric label="Total billed" value={row ? inr.format(Number(row.total_amount)) : "₹0"} /><ClientMetric label="Total paid" value={row ? inr.format(Number(row.paid_amount)) : "₹0"} /><ClientMetric label="Outstanding" value={row ? inr.format(Number(row.outstanding_amount)) : "₹0"} /><ClientMetric label="Portal access" value={`${memberships.data?.length ?? 0} org`} /></div><div className="mt-8 grid gap-6 lg:grid-cols-[1.3fr_.7fr]"><Card><CardHeader><SectionTitle eyebrow="Receivables" title="Outstanding invoices" detail="Client-safe financial information only" /></CardHeader><CardContent>{row ? <div className="rounded-2xl border border-slate-100 p-4"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><p className="font-bold text-slate-900">{row.invoice_number}</p><p className="mt-1 text-xs text-slate-500">{row.billing_period} · Due {row.due_date}</p></div><Badge tone={row.status === "PAID" ? "green" : "amber"}>{row.status}</Badge></div><div className="mt-6 grid grid-cols-2 gap-4 sm:grid-cols-4"><Info label="Invoice date" value={row.invoice_date} /><Info label="Total" value={inr.format(Number(row.total_amount))} /><Info label="Paid" value={inr.format(Number(row.paid_amount))} /><Info label="Outstanding" value={inr.format(Number(row.outstanding_amount))} /></div></div> : <p className="text-sm text-slate-500">No shared invoice records are available.</p>}</CardContent></Card><Card><CardHeader><SectionTitle eyebrow="Privacy" title="Your access" detail="What this portal protects" /></CardHeader><CardContent><div className="space-y-3 text-sm"><PrivacyRow label="Organization scope" value="Own organization" /><PrivacyRow label="Data source" value="RLS-backed" /><PrivacyRow label="Internal notes" value="Not shared" /><PrivacyRow label="Documents" value="Explicitly shared" /></div></CardContent></Card></div><div className="mt-6 rounded-2xl border border-dashed border-blue-200 bg-blue-50/50 p-5 text-sm text-blue-900"><strong>DEMO / SAMPLE workspace.</strong> The figures and invoice shown above are fictional seeded records for testing the client isolation model.</div></>}</div></ClientShell>;
}

function ClientMetric({ label, value }: { label: string; value: string }) { return <Card className="border-blue-100 p-5"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">{label}</p><p className="mt-2 text-2xl font-bold tracking-tight text-slate-950">{value}</p><div className="mt-4 h-1 w-10 rounded-full bg-blue-500" /></Card>; }
function Info({ label, value }: { label: string; value: string }) { return <div><p className="text-[11px] uppercase tracking-wider text-slate-400">{label}</p><p className="mt-1 text-sm font-semibold text-slate-800">{value}</p></div>; }
function PrivacyRow({ label, value }: { label: string; value: string }) { return <div className="flex items-center justify-between border-b border-slate-100 pb-3 last:border-0 last:pb-0"><span className="text-slate-500">{label}</span><span className="font-semibold text-slate-800">{value}</span></div>; }
